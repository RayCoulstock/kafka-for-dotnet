# Keep the image build deliberately small and reliable for GitHub Codespaces.
# Course tools that can tolerate transient network failures are installed by
# .devcontainer/install-tools.sh after the Codespace is created.
FROM mcr.microsoft.com/devcontainers/dotnet:1-8.0-bookworm
